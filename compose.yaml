services:
  server:
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      - DB_USER=user
      - DB_PASSWD=password
      - DB_ADDR=localhost:3306
      - DB_NAME=db
    ports:
      - "8080:80"
  mysql:
    build:
      context: .
      dockerfile: ./sql/docker_init/Dockerfile
    ports:
      - "3306:3306"
    environment:
        MYSQL_ROOT_PASSWORD: mysql
        MYSQL_DATABASE: db
        MYSQL_USER: user
        MYSQL_PASSWORD: password
    restart: always
    networks:
      - backend-network
  cli:
    image: mysql:8.4
    networks:
      - backend-network
    command: mysql -hmysql -uuser -ppassword db

networks:
  backend-network:
    driver: bridge
