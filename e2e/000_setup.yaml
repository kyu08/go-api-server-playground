# NOTE: 最初に実行したいためprefixを付与している
desc: シードデータの投入
runners:
  db: mysql://user:password@mysql:3306/db
steps:
  - include:
      path: zzz_teardown.yaml
  - desc: シードデータの投入
    db:
      query: |-
        insert into user (
          id, screen_name, user_name, bio, is_private, created_at
        ) values (
          '00000000-0000-0000-0000-000000000000', 's_name', 'name', 'bio', false, '2020-01-01 00:00:00'
        );
