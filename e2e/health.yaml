desc: サーバのヘルスチェック
runners:
  req: http://server:8080 # docker composeで立てたserverに対してリクエストを送りたいのでlocalhostではなくservice名を指定している
steps:
  - req:
      /health:
        get:
          body: null
    test: |
      compare(current.res.body, {"message": "ok"})
      && current.res.status == 200
