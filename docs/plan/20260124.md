# Issue #87: query/command をトップレベルで別パッケージにする

## 目的
CQRSを物理的に完全分離し、実装の相互依存を防ぐ。

## 目標構造

```
├── internal
│   ├── command                          # 書き込み系
│   │   ├── domain
│   │   │   ├── user/
│   │   │   │   ├── user.go
│   │   │   │   ├── screen_name.go
│   │   │   │   ├── user_name.go
│   │   │   │   ├── bio.go
│   │   │   │   ├── repository.go
│   │   │   │   └── service.go
│   │   │   ├── tweet/
│   │   │   │   ├── tweet.go
│   │   │   │   ├── body.go
│   │   │   │   └── repository.go
│   │   │   └── entityutil.go            # domain.ID[T] はcommand側のみ
│   │   ├── handler
│   │   │   ├── handler.go               # Handler struct + 初期化
│   │   │   ├── create_user.go           # CreateUser RPC メソッド
│   │   │   └── create_tweet.go          # CreateTweet RPC メソッド
│   │   ├── usecase
│   │   │   ├── user_create.go
│   │   │   └── tweet_create.go
│   │   └── infrastructure
│   │       └── user.go, tweet.go        # Repository実装
│   │
│   ├── query                            # 読み取り系
│   │   ├── handler
│   │   │   ├── handler.go               # Handler struct + 初期化
│   │   │   ├── find_user_by_screen_name.go  # FindUserByScreenName RPC メソッド
│   │   │   └── get_tweet.go             # GetTweet RPC メソッド
│   │   ├── usecase
│   │   │   ├── user_find_by_screen_name.go
│   │   │   └── tweet_get.go
│   │   ├── infrastructure
│   │   │   └── user.go, tweet.go        # Query実装
│   │   ├── query.go                     # Query interface + ReadModel (string ID使用)
│   │   └── health.go                    # Health RPC メソッド
│   │
│   └── shared                           # 共通
│       ├── infrastructure
│       │   ├── database/
│       │   │   ├── connection.go
│       │   │   └── dao/
│       │   └── db.go                    # ReadWriteDB, ReadOnlyDB
│       ├── proto/api/                   # 生成済み pb.go ファイル
│       ├── apperrors/
│       └── grpcutil/
│
├── cmd
│   └── server
│       └── main.go                      # 埋め込みで統合
│
└── proto/                               # 生成元 .proto ファイル
```

## 設計決定

- **proto (*.pb.go)**: `internal/shared/proto/api/` に移動
- **entityutil.go (domain.ID[T])**: `internal/command/domain/` に配置
  - query は string ID を直接使用するよう変更
- **TwitterServer**: 埋め込み (Embedding) 方式で実装

---

## 実装ステップ

### Phase 1: shared パッケージ作成 ✅ 完了

**1.1 ファイル移動**
| 移動元 | 移動先 |
|-------|-------|
| `internal/apperrors/` | `internal/shared/apperrors/` |
| `internal/grpcutil/` | `internal/shared/grpcutil/` |
| `internal/infrastructure/database/connection.go` | `internal/shared/infrastructure/database/connection.go` |
| `internal/infrastructure/database/dao/` | `internal/shared/infrastructure/database/dao/` |
| `internal/domain/repositoryutil.go` | `internal/shared/infrastructure/db.go` |
| `proto/api/*.pb.go` | `internal/shared/proto/api/` |

### Phase 2: command パッケージ作成 ✅ 完了

**2.1 ファイル移動**
| 移動元 | 移動先 |
|-------|-------|
| `internal/domain/user/*.go` | `internal/command/domain/user/` |
| `internal/domain/tweet/*.go` | `internal/command/domain/tweet/` |
| `internal/domain/entityutil.go` | `internal/command/domain/entityutil.go` |
| `internal/handler/create_user.go` | `internal/command/handler/create_user.go` |
| `internal/handler/create_tweet.go` | `internal/command/handler/create_tweet.go` |
| `internal/handler/create_user_test.go` | `internal/command/handler/create_user_test.go` |
| `internal/handler/create_tweet_test.go` | `internal/command/handler/create_tweet_test.go` |
| `internal/usecase/user_create.go` | `internal/command/usecase/user_create.go` |
| `internal/usecase/tweet_create.go` | `internal/command/usecase/tweet_create.go` |
| `internal/infrastructure/database/commandimpl/user.go` | `internal/command/infrastructure/user.go` |
| `internal/infrastructure/database/commandimpl/tweet.go` | `internal/command/infrastructure/tweet.go` |

**2.2 handler.go 新規作成**
`internal/command/handler/handler.go`:
```go
package handler

type Handler struct {
    TweetCreateUsecase *usecase.TweetCreateUsecase
    CreateUserUsecase  *usecase.CreateUserUsecase
}

func NewHandler(client *spanner.Client) *Handler {
    // repository実装の初期化
    // usecase の初期化
}
```

**2.3 RPC メソッドの実装**
`create_user.go`, `create_tweet.go` で `(h *Handler)` のメソッドとして RPC を実装:
```go
// internal/command/handler/create_user.go
func (h *Handler) CreateUser(ctx context.Context, req *api.CreateUserRequest) (*api.CreateUserResponse, error) {
    // usecase 呼び出し
}
```

### Phase 3: query パッケージ作成 ✅ 完了

**3.1 ファイル移動**
| 移動元 | 移動先 |
|-------|-------|
| `internal/query/user.go` | `internal/query/query.go` に統合 |
| `internal/query/tweet.go` | `internal/query/query.go` に統合 |
| `internal/handler/find_user_by_screen_name.go` | `internal/query/handler/find_user_by_screen_name.go` |
| `internal/handler/get_tweet.go` | `internal/query/handler/get_tweet.go` |
| `internal/handler/find_user_by_screen_name_test.go` | `internal/query/handler/find_user_by_screen_name_test.go` |
| `internal/handler/get_tweet_test.go` | `internal/query/handler/get_tweet_test.go` |
| `internal/usecase/user_find_by_screen_name.go` | `internal/query/usecase/user_find_by_screen_name.go` |
| `internal/usecase/tweet_get.go` | `internal/query/usecase/tweet_get.go` |
| `internal/infrastructure/database/queryimpl/user.go` | `internal/query/infrastructure/user.go` |
| `internal/infrastructure/database/queryimpl/tweet.go` | `internal/query/infrastructure/tweet.go` |
| `internal/handler/health.go` | `internal/query/handler/health.go` |
| `internal/handler/health_test.go` | `internal/query/handler/health_test.go` |

**3.2 query.go 変更**
- `domain.ID[user.User]` → `string` に変更
- `domain.ReadOnlyDB` → `shared.ReadOnlyDB` に変更

**3.3 handler.go 新規作成**
`internal/query/handler/handler.go`:
```go
package handler

type Handler struct {
    TweetGetUsecase             *usecase.TweetGetUsecase
    FindUserByScreenNameUsecase *usecase.FindUserByScreenNameUsecase
}

func NewHandler(client *spanner.Client) *Handler {
    // query実装の初期化
    // usecase の初期化
}
```

**3.4 RPC メソッドの実装**
`find_user_by_screen_name.go`, `get_tweet.go` で `(h *Handler)` のメソッドとして RPC を実装:
```go
// internal/query/handler/find_user_by_screen_name.go
func (h *Handler) FindUserByScreenName(ctx context.Context, req *api.FindUserByScreenNameRequest) (*api.FindUserByScreenNameResponse, error) {
    // usecase 呼び出し
}
```

### Phase 4: main.go 更新 (埋め込み方式) ✅ 完了

`cmd/server/main.go`:
```go
package main

import (
    commandhandler "github.com/kyu08/go-api-server-playground/internal/command/handler"
    queryhandler "github.com/kyu08/go-api-server-playground/internal/query/handler"
    "github.com/kyu08/go-api-server-playground/internal/shared/proto/api"
)

type TwitterServer struct {
    api.UnimplementedTwitterServiceServer
    CommandHandler *commandhandler.Handler
    QueryHandler   *queryhandler.Handler
}

func NewTwitterServer(client *spanner.Client) *TwitterServer {
    return &TwitterServer{
        CommandHandler: commandhandler.NewHandler(client),
        QueryHandler:   queryhandler.NewHandler(client),
    }
}

// メソッドの委譲
func (s *TwitterServer) CreateUser(ctx context.Context, req *api.CreateUserRequest) (*api.CreateUserResponse, error) {
    return s.CommandHandler.CreateUser(ctx, req)
}

func (s *TwitterServer) CreateTweet(ctx context.Context, req *api.CreateTweetRequest) (*api.CreateTweetResponse, error) {
    return s.CommandHandler.CreateTweet(ctx, req)
}

func (s *TwitterServer) FindUserByScreenName(ctx context.Context, req *api.FindUserByScreenNameRequest) (*api.FindUserByScreenNameResponse, error) {
    return s.QueryHandler.FindUserByScreenName(ctx, req)
}

func (s *TwitterServer) GetTweet(ctx context.Context, req *api.GetTweetRequest) (*api.GetTweetResponse, error) {
    return s.QueryHandler.GetTweet(ctx, req)
}

func (s *TwitterServer) Health(ctx context.Context, req *api.HealthRequest) (*api.HealthResponse, error) {
    return s.QueryHandler.Health(ctx, req)
}
```

### Phase 5: 旧ディレクトリ削除 ✅ 完了

- `internal/domain/`
- `internal/handler/`
- `internal/usecase/`
- `internal/infrastructure/`
- `internal/query/` (移動後の旧ファイル)

### Phase 6: import パス更新・ビルド・テスト検証 ✅ 完了

すべてのファイルで import パスを新構造に合わせて更新。

---

## 依存関係

```
cmd/server/main.go
    ├── internal/command/handler
    │       ├── command/usecase
    │       │       ├── command/domain
    │       │       └── command/infrastructure → shared/infrastructure/dao
    │       └── shared/proto
    ├── internal/query/handler (Health含む)
    │       ├── query/usecase
    │       │       └── query/infrastructure → shared/infrastructure/dao
    │       └── shared/proto
    └── internal/shared/*

command/* ←✗→ query/*  (相互依存禁止)
```

---

## 検証方法

1. **ビルド確認**
   ```bash
   go build ./...
   ```

2. **テスト実行**
   ```bash
   go test ./...
   ```

3. **相互依存チェック**
   ```bash
   # command が query を import していないことを確認
   go list -f '{{.ImportPath}}: {{.Imports}}' ./internal/command/... | grep query
   # query が command を import していないことを確認
   go list -f '{{.ImportPath}}: {{.Imports}}' ./internal/query/... | grep command
   ```

4. **サーバー起動確認**
   ```bash
   go run ./cmd/server
   ```

---

## 進捗状況

- [x] Phase 1: shared パッケージ作成
- [x] Phase 2: command パッケージ作成
- [x] Phase 3: query パッケージ作成
- [x] Phase 4: main.go 更新
- [x] Phase 5: 旧ディレクトリ削除
- [x] Phase 6: ビルド・テスト検証

## 完了 (2026-01-24)

すべてのフェーズが完了しました。

### 検証結果
- ビルド: ✅ 成功
- テスト: ✅ 全テスト成功
- 相互依存チェック: ✅ command ↔ query 間の依存なし

### コミット履歴
1. refactor: Phase 1 - shared パッケージを作成
2. refactor: Phase 2 - command/domain/user を作成 (途中)
3. refactor: Phase 2 - command パッケージを完成
4. refactor: Phase 3 - query パッケージを作成
5. refactor: Phase 4 - main.go を更新
6. refactor: Phase 5 - 旧ディレクトリを削除
7. docs: CQRS分離の実装計画を追加
8. test: テストファイルを新構造に復元
